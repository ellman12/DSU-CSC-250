BubbleSort(Array, Size)
    flag = 0 //0 means not sorted, 1 means it is sorted MAYBE.
    Do
        i = 0
        flag = 1
        while (i < Size-1)
            if (Array[i] > Array[i+1])
                Swap values at i and i+i
                flag = 0
            i++
    While(flag is 0)
End BubbleSort