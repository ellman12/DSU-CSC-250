Selection Sort Pseudocode

SelectionSort(Array, Size)
    i = 0
    loop i < size-1
        j = i
        sso = Array at j //Smallest So Far
        Loop j < size
            If Array[j] < Array [sso]
                sso = j
        end loop
        swap array[i] with array[sso]
    end outer loop
end Function